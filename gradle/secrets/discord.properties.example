# Copy to .gradle/secrets/discord.properties (gitignored).
# Bot token is read from discord-bot/.env if botToken is not set here.
#
channelId=1508910255738716370
roleIds=1259379750640680961,1259379982187237386
# Optional — overrides discord-bot/.env
# botToken=
# Optional webhook fallback (channelId + botToken preferred)
# webhookUrl=
